describe('Data Type', function () {
    it('should must declare', function () {
        const name: String = "Mamad";
        const belance: Number = 1000;
        const isVip: Boolean = true;

        console.info(name);
        console.info(belance);
        console.info(isVip);
    })
})