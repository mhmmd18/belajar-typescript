"use strict";
describe('object', function () {
    it('should object', function () {
        const person = {
            id: 1,
            name: "Mamad"
        };
        console.info(person.name);
    });
});
