"use strict";
describe('Data Type', function () {
    it('should must declare', function () {
        const name = "Mamad";
        const belance = 1000;
        const isVip = true;
        console.info(name);
        console.info(belance);
        console.info(isVip);
    });
});
